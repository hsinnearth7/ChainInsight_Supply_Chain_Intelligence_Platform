apiVersion: v1
kind: Secret
metadata:
  name: chaininsight-secret
  namespace: chaininsight
  labels:
    app.kubernetes.io/name: chaininsight
type: Opaque
data:
  # IMPORTANT: Replace these placeholder values before deploying!
  # Generate real secrets: echo -n 'YOUR_SECRET' | base64
  API_KEY: REPLACE_WITH_BASE64_ENCODED_VALUE
  DATABASE_URL: REPLACE_WITH_BASE64_ENCODED_VALUE
  REDIS_URL: REPLACE_WITH_BASE64_ENCODED_VALUE
